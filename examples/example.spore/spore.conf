FORMAT=1
HOST=galadriel
MODULES="repos net users ssh storage dufs firewall"
# Where the age private key lives on the host. This is the one thing a spore
# cannot carry — it is what unlocks everything the spore does carry.
SECRETS_IDENTITY=/etc/spore/identity
