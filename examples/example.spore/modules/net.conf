NET_HOSTNAME=changeme
# A name here is a claim about hardware you are not looking at. `auto` — which
# is also the default if this line is absent — takes eth0 when the machine has
# one and its single other card when it does not. Name it only when the machine
# has more than one and you mean a particular one.
NET_IFACE=eth0
NET_MODE=dhcp
